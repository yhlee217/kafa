"""감독형 수집(RPA) — 사람이 로그인한 브라우저를 이어받아 반복 클릭만 자동화.

목적: 여러 거래처·여러 달의 다운로드본을 손으로 받는 반복 작업을 줄인다.
이력이 쌓일수록 미추천 자동 해소율이 오른다(실측: 이력 있으면 96% 정확도).

**설계 원칙 — 안전선**
- **자격증명을 코드가 만지지 않는다.** 아이디·비밀번호·공동인증서·OTP 자동 입력 없음.
  사람이 직접 로그인하고, 스크립트는 그 세션을 이어받아 화면 조작만 한다.
- **비공개 API 역공학 없음.** 사람이 화면에서 하는 것과 같은 동작만 한다.
- **사람이 켜고 지켜본다.** 무인 상시 실행이 아니라 필요할 때 실행하는 도구.
- **요청 간 간격**을 둬서 서버에 부담을 주지 않는다(config `delay_seconds`).

**이용약관 확인은 사용자 책임이다.** 서비스 약관이 자동화(로봇/스크립트 접근)를 금지한다면
이 도구를 쓰면 안 된다. 실행 시 이 사실을 고지한다.

화면 선택자(selector)는 `config/fetch/wehago.yaml` 로 외부화한다 — 화면이 바뀌어도
코드 수정 없이 대응하고, 실제 화면을 보고 한 번 보정(calibration)해야 동작한다.
"""
from kafa.fetch.plan import DownloadPlan, DownloadTask, build_plan, target_path

__all__ = ["DownloadPlan", "DownloadTask", "build_plan", "target_path"]
